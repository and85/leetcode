using System.Text;

public class Solution {
    public string DecodeString(string s) 
    {
        return DecodeRecursive(s, 0).ToString();
    }

    private StringBuilder DecodeRecursive(string s, int start)
    {
        var sb = new StringBuilder();

        for (int i = 0; i < s.Length; i++)
        {

        }

        return sb;
    }
}